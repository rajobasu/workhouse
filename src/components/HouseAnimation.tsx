import React from "react";

interface Props {
  progressValue: number;
  targetValue: number;
}

export const HouseAnimation: React.FC<Props> = ({
  progressValue,
  targetValue,
}) => {
  return <></>;
};
