import React from "react";
import { Text, WhiteBaseContainer } from "../css/css";

export const MetaMask: React.FC = () => {
    return (
        <WhiteBaseContainer>
            <Text>This is the page to connect the metamask wallet to react app.</Text>
        </WhiteBaseContainer>
    );
};
